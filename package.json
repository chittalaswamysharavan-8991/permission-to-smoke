{
  "name": "permission-to-smoke",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "A privacy-first PWA that adds a deliberate pause before smoking decisions.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build",
    "serve": "python3 -m http.server 4173 -d dist"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
